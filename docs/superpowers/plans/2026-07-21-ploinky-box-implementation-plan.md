# Ploinky Box Implementation Plan

Date: 2026-07-21

Status: ready-to-implement plan derived solely from the approved invariant
register `/Users/danielsava/work/file-parser/ploinky/docs/superpowers/specs/2026-07-21-ploinky-box-invariants.md`
(the sole normative input). Repo root: `/Users/danielsava/work/file-parser/ploinky`,
branch `ploinky-proxy`. Shell commands run from the repo root unless stated.

Every invariant ID (N1–N7, LAY-*, BIN-*, PRT-*, IDN-*, IMG-*, VOL-*, NST-*,
CMD-*, RTE-*, MAN-*, PUB-*, Q1–Q6, VER-1..VER-16) refers to the register.
Nothing in this plan is sourced from superseded documents, deleted DS specs, or
retired branches.

## 0. Baseline discipline (read before Task T01)

| Rule | Detail |
| --- | --- |
| Dirty-tree hazard | On 2026-07-21 the `ploinky-proxy` working tree carried uncommitted modifications under `/Users/danielsava/work/file-parser/ploinky/cli/` and `/Users/danielsava/work/file-parser/ploinky/tests/` from unrelated work (observed via `git status`). Box work must start from a committed clean baseline or those changes will pollute the VER-2 measurement. |
| `BOX_BASE_SHA` | Before T01: land or park all unrelated changes, then record `BOX_BASE_SHA=$(git -C /Users/danielsava/work/file-parser/ploinky rev-parse HEAD)` in the work log. Every VER-2 diff gate in this plan compares against this SHA. |
| Additive boundary | Zero edits under `/Users/danielsava/work/file-parser/ploinky/cli/`, `/Users/danielsava/work/file-parser/ploinky/Agent/`, `/Users/danielsava/work/file-parser/ploinky/dashboard/` (N5). Implementation lives only under `/Users/danielsava/work/file-parser/ploinky/ploinky-box/`. Packaging touches only `/Users/danielsava/work/file-parser/ploinky/bin/*` and `/Users/danielsava/work/file-parser/ploinky/package.json`. Test files are additive under `/Users/danielsava/work/file-parser/ploinky/tests/` (see decision D1). |
| Gate command | `git -C /Users/danielsava/work/file-parser/ploinky diff --name-only "$BOX_BASE_SHA"..HEAD -- cli/ Agent/ dashboard/` must print nothing, at every phase exit and at release. |

## 1. Verified code anchors (Observed, 2026-07-21)

All claims below were read directly from the working tree at commit `46fc450`
on 2026-07-21. Line numbers may drift; re-verify before citing them in code
review.

| # | Anchor | Evidence |
| --- | --- | --- |
| A1 | Router port default `8080`; public bind default `127.0.0.1`; authority mandatory on wildcard bind | `/Users/danielsava/work/file-parser/ploinky/cli/server/RoutingServer.js:80-86` |
| A2 | Router `/health` endpoint exists | `/Users/danielsava/work/file-parser/ploinky/cli/server/RoutingServer.js:601` |
| A3 | Private listener pinned `127.0.0.1:8081` | `/Users/danielsava/work/file-parser/ploinky/cli/server/privateListener.js:10-11` |
| A4 | Host-sandbox kill switch env name | `/Users/danielsava/work/file-parser/ploinky/cli/utils/runtime/sandboxRuntime.js:3` |
| A5 | Engine probe order podman-before-docker | `/Users/danielsava/work/file-parser/ploinky/cli/sandbox/docker/common.js:85-95` |
| A6 | Nested container naming `ploinky_<repo>_<agent>_<projectDir>_<cwdHash>` | `/Users/danielsava/work/file-parser/ploinky/cli/sandbox/docker/common.js:193-204` |
| A7 | `requireContainerRuntime()` exits when no engine on PATH | `/Users/danielsava/work/file-parser/ploinky/cli/sandbox/docker/common.js:97-104` |
| A8 | Today's `bin/ploinky`: achillesAgentLib check, `-shell`/`sh`/`--shell` routing to `bin/ploinky-shell`, exec of `cli/index.js` | `/Users/danielsava/work/file-parser/ploinky/bin/ploinky:7-23` |
| A9 | `bin/p-cli` declares itself an alias and execs `bin/ploinky` | `/Users/danielsava/work/file-parser/ploinky/bin/p-cli:3-8` |
| A10 | `bin/psh` execs `bin/ploinky sh` | `/Users/danielsava/work/file-parser/ploinky/bin/psh:5` |
| A11 | `bin/ploinky-shell` runs `cli/shell.js` directly, no engine involvement | `/Users/danielsava/work/file-parser/ploinky/bin/ploinky-shell:5-7` |
| A12 | `package.json` bin map holds only stale `p-cloud`; `"type": "module"`; `"test": "./tests/run-all.sh"` | `/Users/danielsava/work/file-parser/ploinky/package.json:5,7-9,13` |
| A13 | Bare CLI invocation opens the interactive REPL; args dispatch through `handleCommand` | `/Users/danielsava/work/file-parser/ploinky/cli/index.js:432-443` |
| A14 | CLI-side achillesAgentLib presence check | `/Users/danielsava/work/file-parser/ploinky/cli/index.js:21-29` |
| A15 | Core verbs today: `cli` (requires agent arg), `start`, `status`, `shutdown`, `stop` (router + configured agents), `destroy` | `/Users/danielsava/work/file-parser/ploinky/cli/commands/cli.js:201-204,338,400-401,628-660` |
| A16 | `start` positional grammar `[staticAgent, port]` | `/Users/danielsava/work/file-parser/ploinky/cli/utils/repos.js:595-625` |
| A17 | Test layout: `tests/unit`, `tests/integration`, `tests/e2e`, `tests/fixtures` exist; `npm test` → `tests/run-all.sh` → `tests/test_all.sh` | `ls /Users/danielsava/work/file-parser/ploinky/tests/`; `/Users/danielsava/work/file-parser/ploinky/tests/run-all.sh:5` |
| A18 | Unit stage auto-discovers `tests/unit/*.test.{js,mjs,cjs}` at depth 1 via `node --test` | `/Users/danielsava/work/file-parser/ploinky/tests/test_all.sh:192-210` |
| A19 | Staged suite invokes `ploinky` by bare name from PATH | `/Users/danielsava/work/file-parser/ploinky/tests/doPrepare.sh:265` and passim |
| A20 | `tests/integration/*.test.mjs` exist but are not wired into `test_all.sh`; they run directly via `node --test` | `grep -rn integration /Users/danielsava/work/file-parser/ploinky/tests/test_all.sh` (comment-only match) |
| A21 | `container/` layout absent from this branch | `git -C /Users/danielsava/work/file-parser/ploinky ls-files container/` → 0 files |
| A22 | `cli/services/` absent from this branch | `ls /Users/danielsava/work/file-parser/ploinky/cli/services` → no such directory |
| A23 | No `router-health.sock` surface on this branch | `grep -rn "router-health" cli/ Agent/` → no matches |
| A24 | Master key sourced from `PLOINKY_MASTER_KEY` env or key-value secret files | `/Users/danielsava/work/file-parser/ploinky/cli/utils/security/masterKey.js:5` |
| A25 | Repo root has no `ploinky-box/`; no file under `cli/`, `Agent/`, `dashboard/`, `bin/` references `ploinky-box` | `ls` + `grep -rl ploinky-box cli/ Agent/ dashboard/ bin/` → none |
| A26 | Publish workflow: manual dispatch, seven 40-char SHA inputs, native amd64+arm64 gate jobs, merge job moves `runtime` tag; gates contract `5` at `sources/ploinky/container/runtime-contract.mjs`; image metadata table (user `podman`, workdir `/workspace`, entrypoint `/usr/local/bin/ploinky-box-entrypoint`, empty cmd, no volumes, exactly 8 env vars); in-box marker file `/etc/ploinky-box`; expects `/opt/ploinky/bin/ploinky-install-deps` in mounted source; stale references to `cli/services/*` and `/workspace/.ploinky/run/router-health.sock` | `/Users/danielsava/work/file-parser/container-image-builds/.github/workflows/publish-ploinky-box-image.yml:4-33,184-207,246,260-288,322,351,373,420,455,591` (read-only exception) |

Delegated/prompt-provided (not re-verified locally): the published
`docker.io/assistos/ploinky-box:runtime` tag carried contract label `3` on
2026-07-21 (Docker Hub API inspection cited by the planning prompt). The plan
treats every previously published contract (`3`, and any historical `4`/`5`)
as incompatible per IMG-2 regardless of the exact live value.

## 2. Resolved decisions

### 2.1 Open questions the register assigns to this plan

| ID | Decision | Rationale | Label |
| --- | --- | --- | --- |
| Q2 | Runtime-contract version value is **`6`**, under the unchanged label key `io.assistos.ploinky.runtime-contract`. | Must differ from every previously published label including `3` (live tag) and from `5` (currently gated by the workflow, A26); the workflow's own gate text also references a historical contract `4`. Keeping the label *key* stable lets contract-6 code read the version off any older image/box and emit the IMG-2 destroy-and-recreate diagnostic by value comparison. Paired with the cross-repo gate-path migration `container/` → `ploinky-box/` in the same change (section 8). | proposed — owner may override |
| Q3a | `bin/p-cli` routes **boxed**: it stays byte-identical to today (exec of `bin/ploinky`, A9) and therefore follows `ploinky` into the passthrough. | Its own header declares it "an alias to ploinky command" (`/Users/danielsava/work/file-parser/ploinky/bin/p-cli:3`); an alias must track the aliased command. Tested in T21. | proposed — owner may override |
| Q3b | `bin/psh` routes **local**: rewritten to exec `bin/ploinky-local sh "$@"`. | `psh` is a shortcut to the LLM-recommendation shell (`cli/shell.js`, A10/A11), a host-side advisory UX with no container dependency; requiring a box for it would make the cheapest command the most expensive. The boxed variant stays reachable as `ploinky sh` (forwarded core token, handled in-box by `bin/ploinky-local`'s BIN-1 shell routing). Tested in T21. | proposed — owner may override |
| Q3c | `bin/ploinky-shell` routes **local by construction**: unchanged; it runs `cli/shell.js` directly (A11) and remains the target of `ploinky-local`'s `-shell`/`sh`/`--shell` routing (BIN-1). | No engine involvement exists in it today; BIN-1 freezes the routing that reaches it. Tested in T21. | proposed — owner may override |
| Q4 | In-box REPL is **today's `cli/index.js` REPL invoked as-is** — bare `ploinky` reconciles/starts the box, then runs an interactive engine exec of `/opt/ploinky/bin/ploinky-local` with no arguments (TTY allocated). No wrapper, no prompt changes. | Bare invocation already opens the interactive mode (A13); CMD-3 fixes observable behavior only; spawn-not-import keeps LAY-2 one-way and N5 intact. | proposed — owner may override |

### 2.2 Decisions fixed during planning (D-series)

| ID | Decision | Rationale | Label |
| --- | --- | --- | --- |
| D1 | Test placement: unit tests are additive flat files `tests/unit/ploinkyBox*.test.mjs` (auto-discovered, A18); integration tests `tests/integration/ploinkyBox*.test.mjs` (direct `node --test`, A20); e2e runner `tests/e2e/ploinkyBox/run.mjs`; fixtures `tests/fixtures/ploinky-box/`; the workflow-invoked smoke harness lives under `ploinky-box/smoke/` (LAY-1 "box smoke tests"). | Register section 16 requires every check to be "runnable in this repository's test layout (`tests/unit`, `tests/integration`, `tests/e2e`, `npm test` → `tests/run-all.sh`)", which necessarily adds files under `tests/`. This is reconciled with N5/VER-2's stricter letter in section 10 (discrepancy R1). No existing test file is modified; no harness file is modified (unit discovery is automatic). | fixed by register §16; interpretation flagged R1 |
| D2 | Forwarded-argv exception for `start` ports: when the outer layer accepts a router-port form (prefix `--port PORT` or positional `start AGENT PORT`, CMD-5), the port selects only `<routerHostPort>`; the forwarded core command always carries `8080` in the positional port slot (`start AGENT 8080`). A `start` with no port form is forwarded unchanged. Every other token of every command is byte-exact (CMD-2). | PRT-1/PRT-6 pin the box-side listener to `8080`; forwarding the host port into the box would move the in-box router off the constant the publication maps to. CMD-5 explicitly carves start's port grammar out of plain forwarding. | fixed by CMD-5 + PRT-1/PRT-6; flagged R4 |
| D3 | In-box marker (BIN-4/IMG-8) is the image-baked file `/etc/ploinky-box`, whose content is the contract version string (`6`). One detection helper `isInsideBox()` in `ploinky-box/lib/boxMarker.mjs`; no other environment sniffing. | The currently published image already bakes exactly this marker (A26, workflow line 322), minimizing image churn; a file is inspectable and cannot be shadowed by inherited environment. | proposed — owner may override |
| D4 | Forwarded commands exec `/opt/ploinky/bin/ploinky-local` inside the box explicitly. The marker path (D3) additionally protects any manual in-box `ploinky` invocation from recursion. | Direct target avoids a double shim hop; both paths are VER-5-tested. In-box PATH `/opt/ploinky/bin:...` (A26) keeps `ploinky`/`ploinky-local` resolvable for interactive use. | proposed — owner may override |
| D5 | Host-side per-identity mutation lock (IDN-8) lives at `~/.ploinky-box/locks/<instance>.lock` (mode 0700 dir), JSON body `{pid, hostname, createdAt}`. Stale recovery removes a lock only after `process.kill(pid, 0)` proves ESRCH for a same-host owner; foreign-host locks are never auto-removed. `help`/`status` never touch it. | Must be host-side and outside any workspace so the create path reads no workspace state (PRT-4, VER-2). | proposed — owner may override |
| D6 | Box-owned label namespace `io.assistos.ploinky-box.*`: `schema=1`, `path-hash=<12hex>`, `role=box\|workspace\|containers\|ploinky-deps`, `image-ref=<requested logical reference>` (IMG-4), `router-host-port=<n>`. The cleartext absolute path never appears (IDN-7). The image's contract label key stays `io.assistos.ploinky.runtime-contract` (Q2). | Distinct namespace keeps box ownership metadata separate from nested-agent conventions; `router-host-port` label lets reconciliation detect configuration changes that require IMG-5 replacement (CMD-6). | proposed — owner may override |
| D7 | Supervisor modules accept engine-runner and image-reference injection through their programmatic API only (used by unit tests and the smoke harness). No public CLI flag and no environment override exists for engine selection or instance naming (IDN-3). Default image reference constant: `docker.io/assistos/ploinky-box:runtime`. | Preserves IDN-3 while making VER-1/6/7/9 testable without a real engine and letting the workflow smoke run against digest refs. | fixed by IDN-3; injection shape proposed |
| D8 | An outer option `--dry-run` prints the fully resolved engine command plan (create/start argv, publications, volumes, labels, env) without executing anything. | The register's section-14 exclusion list names "dry-run output" as a secret-free surface, implying the capability; it is also the cheapest runnable probe for VER-1 and VER-15. | proposed — owner may override |
| D9 | `package.json` changes are limited strictly to the `bin` map (BIN-5). No npm scripts are added; the e2e runner is invoked as `node /Users/danielsava/work/file-parser/ploinky/tests/e2e/ploinkyBox/run.mjs`. | N5's letter permits only the bin map inside `package.json`. | fixed by N5/BIN-5 |
| D10 | A new auxiliary `bin/ploinky-install-deps` (in-box helper: installs core dependencies into the writable deps volume from the read-only `/opt/ploinky` mount) is added as a packaging file. Routing assignment: in-box use only; it is a no-op guard failure outside a box (marker check). | IMG-6 mounts core read-only with a dedicated writable `node_modules` volume; plain `npm install` would attempt writes to the read-only root. The currently gated image already expects this helper at `/opt/ploinky/bin/ploinky-install-deps` (A26, workflow line 351), so the name keeps image-side churn at zero. `bin/*` is inside the approved packaging surface (VER-2). | proposed — owner may override |
| D11 | Boxed workspace semantics: the box workspace is the named volume `<instance>-workspace` mounted at `/workspace` (VOL-1), keyed by host-cwd identity (IDN-1). The host directory's own files (including any host-side `.ploinky/`) are not mounted into the box; only `ploinky-local` operates on them. | Direct consequence of IDN-1 + VOL-1 + the section-4 workspace seam; stated explicitly because it is user-visible. | fixed by register; stated for clarity |

### 2.3 Deferred items (nothing below may be contradicted)

| Item | Constraint that must hold when it lands |
| --- | --- |
| PUB-1..PUB-5 (publication/tunnel slice) | Out of build scope. The base plan keeps local-only as a mode (not a failure), adds no inbound public TCP surface, no wildcard host TCP bind (PRT-2), and leaves the connector binary an image concern (the current image already carries `cloudflared`; the contract-6 metadata table in T06 deliberately does not forbid it). No base-slice config field may occupy or preclude the publication slice's desired-state keys. |
| Q5 (UDP media consumer) | Out of scope. The reserved `0.0.0.0:7882:7882/udp` mapping exists from day one unconditionally (PRT-3, T07); an idle mapping that drops packets is the accepted steady state until the media slice lands. |
| Q1 (bridge-nested agent → private listener `8081`) | Open; owned by the routing layer. No task in this plan depends on it (section 10). |

## 3. Phase sequence

| Phase | Slice | Depends on | Entry gate | Exit gate |
| --- | --- | --- | --- | --- |
| 1 | Scaffolding, constants, identity, engine discovery, mutation lock, boundary guard | — | `BOX_BASE_SHA` recorded; clean committed baseline | `node --test tests/unit/ploinkyBoxConstants.test.mjs tests/unit/ploinkyBoxIdentity.test.mjs tests/unit/ploinkyBoxEngineDiscovery.test.mjs tests/unit/ploinkyBoxMutationLock.test.mjs tests/unit/ploinkyBoxBoundary.test.mjs` → all pass; VER-2 diff gate empty |
| 2 | Runtime-contract validation, publications, volumes, image lifecycle, supervisor core (fake-engine tier) | 1 | Phase 1 exit green | `node --test tests/unit/ploinkyBoxRuntimeContract.test.mjs tests/unit/ploinkyBoxPublications.test.mjs tests/unit/ploinkyBoxVolumes.test.mjs tests/unit/ploinkyBoxImageLifecycle.test.mjs tests/unit/ploinkyBoxSupervisor.test.mjs tests/unit/ploinkyBoxSecretHygiene.test.mjs` → all pass |
| 3 | Outer grammar, passthrough, recursion break, forwarding | 1, 2 | Phase 2 exit green | `node --test tests/unit/ploinkyBoxArgGrammar.test.mjs tests/unit/ploinkyBoxCommandRouting.test.mjs` → all pass (includes the VER-4 collision matrix and the VER-5 marker unit) |
| 4 | Host-special verbs, lock integration, secret canaries | 2, 3 | Phase 3 exit green | `node --test tests/unit/ploinkyBoxStartFlow.test.mjs tests/unit/ploinkyBoxStopStatus.test.mjs tests/unit/ploinkyBoxDestroy.test.mjs tests/unit/ploinkyBoxLockMatrix.test.mjs tests/unit/ploinkyBoxSecretCanaries.test.mjs` → all pass |
| 5 | Packaging: `bin/*` rewiring + `package.json` bin map | 3, 4 | Phase 4 exit green (passthrough must exist before `bin/ploinky` points at it) | T21 byte-checks pass; T22 VER-3 parity run green; VER-2 diff gate empty |
| 6 | Entrypoint assets, smoke harness, real-engine integration tests | 1–4 (5 for e2e runner ergonomics) | Phase 5 exit green; host with rootless podman available for integration tier | `bash -n` on entrypoint; `node --test tests/unit/ploinkyBoxEntrypoint.test.mjs` pass; `node --test tests/integration/ploinkyBoxEngineMatrix.test.mjs tests/integration/ploinkyBoxReservedPorts.test.mjs` pass on a podman host |
| 7 | Cross-repo coordination (planned here, executed in `container-image-builds` with owner approval) | 6 | All Phases 1–6 merged; ploinky SHA pinned | Cross-repo change reviewed/approved by owner; workflow dispatch executed per N7; both native gates + merge job green |
| 8 | Release gate | 7 | `runtime` tag moved by the merge job | VER-12 local e2e pass; full `npm test` pass; final VER-2 diff gate empty; VER matrix sign-off (section 7) |

Order justification: the spine follows dependency cost. Phases 1–4 are
implementable and fully unit-verifiable with no engine and no image (fake-engine
injection, D7), so they front-load all architectural risk that can be retired
cheaply. Packaging (Phase 5) must wait until the passthrough exists, or
`bin/ploinky` would point at nothing and every PATH consumer (including the
staged test suite, A19) would break mid-stream. Entrypoint and smoke assets
(Phase 6) are the payload of the cross-repo change, so they must be merged and
SHA-pinned before Phase 7 can reference them; product-image end-to-end checks
(Phase 8) are impossible before the first contract-6 publish because no
compatible published image exists (section 1, delegated fact; IMG-2).

## 4. Task breakdown

Conventions for every task: file paths are absolute; "unit" means the file is
auto-discovered by the NODE UNIT STAGE (A18) and individually runnable as
shown; fake-engine tests inject a recording engine runner (D7) and never touch
a real engine; all new modules are ESM (`"type": "module"`, A12) using Node
built-ins only (LAY-4).

### Phase 1 — scaffolding, identity, discovery, lock

| Task | Content |
| --- | --- |
| **T01** | **Scaffolding + constants + marker helper.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/constants.mjs` exporting: `ROUTER_PORT = 8080`, `PRIVATE_LISTENER_PORT = 8081`, `UDP_RESERVED_PORT = 7882` (PRT-11, matching A1/A3), `REQUIRED_RUNTIME_CONTRACT = '6'` (Q2), `CONTRACT_LABEL_KEY = 'io.assistos.ploinky.runtime-contract'`, `DEFAULT_IMAGE_REF = 'docker.io/assistos/ploinky-box:runtime'`, box label keys (D6), volume suffixes/mount targets (`-workspace` → `/workspace`, `-containers` → `/home/podman/.local/share/containers`, `-ploinky-deps` → `/opt/ploinky/node_modules`; VOL-1), `IN_BOX_MARKER_PATH = '/etc/ploinky-box'` (D3), stop timeouts (in-box graceful 60 s, engine stop 20 s). Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/boxMarker.mjs` with the single `isInsideBox()` helper (BIN-4). Deps: none. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxConstants.test.mjs` (new file, also created here) asserts the exported values above, asserts `8081` appears in no publication constant, and asserts by string scan that `ploinky-box/lib/*.mjs` never imports from `/Users/danielsava/work/file-parser/ploinky/cli/` (LAY-2). Expected: pass. |
| **T02** | **Identity derivation.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/identity.mjs`: `deriveIdentity(cwd)` → `canonicalPath = fs.realpathSync(cwd)`, `pathHash =` first 12 lowercase hex of SHA-256(canonicalPath), `slug =` sanitized bounded basename (lowercase, `[a-z0-9-]` only, runs collapsed, trimmed to ≤ 20 chars, fallback `box` when empty), `instance = ploinky-box-<slug>-<pathHash>`, plus derived volume names (IDN-1). Returns identity only; performs no engine or filesystem writes. Deps: T01. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxIdentity.test.mjs` — creates temp dirs: symlink and target resolve to the same instance; equal basenames at different paths differ; renamed dir yields a new identity; hash is 12 lowercase hex (IDN-2). Expected: pass. |
| **T03** | **Engine client + discovery.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/engineClient.mjs` (zero-dep `spawnSync`/`spawn` wrapper with argv capture hooks for tests, D7) and `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/engineDiscovery.mjs`: for each of `podman`, `docker` independently, probe binary presence (PATH) then daemon reachability (`<engine> info`), then enumerate resources labeled with this identity's `path-hash`. Published per-engine result: `absent` (not installed), `unknown` (installed but unreachable — never `absent`, IDN-4), `owns` (reachable, ≥ 1 owned resource), or `reachable-empty` (reachable, zero owned resources — the state IDN-6's "completely empty identity" requires; see discrepancy R3). Selection (IDN-6): sole owner wins; split/foreign ownership fails closed printing both inventories; podman-first only when every reachable engine is `reachable-empty`. Blocking rule (IDN-5) exposed as `assertNoUnknownEngines(action)`. Deps: T01, T02. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxEngineDiscovery.test.mjs` — fake-engine matrix: installed-unreachable → `unknown` and every mutating action refuses before any engine mutation call while `help` path stays callable; sole-owner selection; split ownership fails closed with both inventories in the error; podman-first on empty identity only (VER-9 unit tier). Expected: pass. |
| **T04** | **Mutation lock.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/mutationLock.mjs` per D5: `withMutationLock(instance, fn)` acquiring via `O_EXCL` create under `~/.ploinky-box/locks/`, releasing on exit; stale recovery only after proving the recorded same-host owner PID is gone (ESRCH); bounded retry/backoff; `status`/`help` never call it (IDN-8). Deps: T01, T02. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxMutationLock.test.mjs` — two concurrent child processes serialize on one identity (observable interleaving log); a lock naming a dead PID is recovered exactly once; a lock naming a live PID is honored until released (VER-14 unit tier). Expected: pass. |
| **T05** | **Additive-boundary guard.** Create `/Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxBoundary.test.mjs`: (a) static import check — no file under `cli/`, `Agent/`, `dashboard/` contains the string `ploinky-box` (VER-2; trivially true today, A25, and locked in from now on); (b) create-path purity — the modules on the create path (`identity.mjs`, `publications.mjs` once T07 lands, `supervisor.mjs` create branch) contain no `.ploinky` literal and no manifest/profile import (VER-2, PRT-4, MAN-1); (c) LAY-4 proof — copy `/Users/danielsava/work/file-parser/ploinky/ploinky-box/` to a temp dir outside any `node_modules` ancestry and dynamically import every module there with an empty `node_modules`; imports must succeed. Deps: T01–T04 (extends as later modules land). |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxBoundary.test.mjs` → pass, plus the phase-exit diff gate `git -C /Users/danielsava/work/file-parser/ploinky diff --name-only "$BOX_BASE_SHA"..HEAD -- cli/ Agent/ dashboard/` → empty output. |

### Phase 2 — contract, publications, volumes, image lifecycle, supervisor

| Task | Content |
| --- | --- |
| **T06** | **Runtime-contract validation.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/runtimeContract.mjs`: one authoritative metadata table for contract `6` — `User=podman`, `Home=/home/podman`, `WorkingDir=/workspace`, `PATH=/opt/ploinky/bin:/usr/local/bin:/usr/bin`, `Entrypoint=["/usr/local/bin/ploinky-box-entrypoint"]`, empty `Cmd`, no image-declared `Volumes`, exactly 8 env vars (`PATH`, `USER=podman`, `HOME=/home/podman`, `PLOINKY_WORKSPACE_ROOT=/workspace`, `PLOINKY_DISABLE_HOST_SANDBOX=1` (A4, IMG-8), `container=oci`, `_CONTAINERS_USERNS_CONFIGURED=` (empty), `BUILDAH_ISOLATION=chroot`), label `io.assistos.ploinky.runtime-contract=6`. Values mirror the currently gated image metadata (A26) with only the version bumped, minimizing image churn; the same table is the source for the cross-repo gates (T27). `validateImageMetadata(inspectJson)` checks field by field with field-specific errors; the label alone is never sufficient (IMG-1). `assertSupportedContract(labelValue)` produces the IMG-2 fail-closed error whose text instructs `ploinky destroy` + recreate and mutates nothing. Add golden + mutated inspect-JSON fixtures under `/Users/danielsava/work/file-parser/ploinky/tests/fixtures/ploinky-box/`. Deps: T01. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxRuntimeContract.test.mjs` — for each single-field mutation of the golden fixture the validator rejects naming exactly that field; contract label `3` (and `5`) produce the destroy-and-recreate instruction and no engine calls (VER-6). Expected: pass. |
| **T07** | **Publications constructor (publish formatter).** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/publications.mjs`: `buildPublications({routerHostPort = 8080})` unconditionally returns exactly `['-p', '127.0.0.1:<routerHostPort>:8080/tcp', '-p', '0.0.0.0:7882:7882/udp']` (PRT-1, PRT-3); it is a pure function with no workspace/manifest/profile/engine input (PRT-4, PRT-5). `mergeCreateConfig()` has no extra-publication field of any kind and rejects any attempted override of the UDP reservation (PRT-5, PRT-7). `diagnoseUdpCollision(engineError)` maps an engine port-in-use failure on `7882/udp` to the fixed-port collision diagnostic, naming the owner where `lsof -nP -iUDP:7882` yields one (PRT-8); the box never remaps. Deps: T01. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxPublications.test.mjs` — exactly two `-p` mappings for default and custom `routerHostPort`; `--port` alters only the host TCP side; the module's API accepts no publication list; UDP override attempts throw; collision mapping produces the diagnostic text (VER-1, VER-10 unit tier). Expected: pass. |
| **T08** | **Volumes.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/volumes.mjs`: `ensureVolumes(identity)` explicitly creates the three named volumes with the full D6 label set before first attachment (VOL-1); `classifyVolume(inspectJson)` marks exact-named volumes with missing/malformed/mismatched labels as foreign — never attach, relabel, migrate, or delete (VOL-2); reattachment logic for recreation (VOL-4). Deps: T01–T03. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxVolumes.test.mjs` — fake engine: creation emits three labeled `volume create` calls before any `create` that mounts them; a foreign exact-name volume aborts creation fail-closed with no `volume rm`/relabel call recorded (VER-8 unit tier). Expected: pass. |
| **T09** | **Image lifecycle.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/imageLifecycle.mjs`: `pullPolicy(operation)` — pull unconditionally before create and before intentional replacement; never for reuse/stopped-start/`status`/`stop`/`destroy`; pull failure never falls back to a cached tag (IMG-3). Post-pull, resolve and run by image ID, recording the requested logical reference in the D6 label (IMG-4). `replaceBox()` transaction: snapshot old container inspect → resolve desired config → pull → validate (T06) → graceful core stop (T17 sequence) → remove old box → create from validated ID with the same named volumes → health-validate (running + entrypoint `PLOINKY-BOX-SELF-CHECK-OK` log line within timeout) → on any failure remove the replacement and recreate from the snapshot's image ID and creation config (IMG-5). Deps: T01–T03, T06–T08. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxImageLifecycle.test.mjs` — fake-engine pull matrix: create/replace record exactly one pull then run-by-ID; reuse, stopped-start, status, stop, destroy record zero registry calls; failed replacement restores prior image ID and config (VER-7). Expected: pass. |
| **T10** | **Supervisor.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/supervisor.mjs` composing T02–T09 into lifecycle ops: `ensureBox` (reuse running compatible → start stopped compatible → create missing → IMG-2 error on incompatible → IMG-5 replacement when recorded `router-host-port`/image config differs, CMD-6), `stopBox`, `destroyBox`, `boxStatus`, `planCreate` (dry-run data, D8). Create args are fixed: `--name <instance> --user podman --device /dev/fuse --device /dev/net/tun --security-opt unmask=ALL`, the two publications (T07), `-e PLOINKY_PUBLIC_BIND=0.0.0.0 -e PLOINKY_PUBLIC_AUTHORITY=127.0.0.1:<routerHostPort>` (PRT-10; wildcard bind inside the box requires the authority, A1 — exact authority string verified against `normalizeAuthority` behavior at VER-11 time, risk R7), `-v <repoRoot>:/opt/ploinky:ro` plus the three named volumes (IMG-6, VOL-1), D6 labels, then the resolved image ID. Never `--privileged` (IMG-7), never an engine-socket mount (NST-3), never a cleartext path label (IDN-7), no wildcard host TCP bind (PRT-2). `ploinky-local` inherits today's defaults untouched (PRT-10). Deps: T02–T09. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxSupervisor.test.mjs` — fake-engine argv assertions: exactly two `-p` entries with no workspace present, with an empty workspace dir, and with a populated workspace dir (constructor provably reads none of them — VER-1); argv contains no `--privileged`, no `docker.sock`/`podman.sock` mount, no `8081` mapping (VER-10 unit tier), both PRT-10 env vars; labels carry hash+roles but never the cleartext path. Expected: pass. |
| **T11** | **Status formatting + secret hygiene.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/statusReport.mjs` (outer box state block + forwarded core-status block, machine-greppable) and `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/secretHygiene.mjs`: the create/exec env allowlist is exactly the two computed non-secret PRT-10 variables; host environment is never forwarded wholesale; no secret value may enter engine argv, container env, labels, status, dry-run, or logs (section-14 exclusion; master key stays workspace-volume-internal, sourced by in-box core per A24). Deps: T10. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxSecretHygiene.test.mjs` — with `PLOINKY_MASTER_KEY` and a poisoned `.env`-style canary in the host env, fake-engine create/start/status/dry-run surfaces contain no canary substring (VER-15 unit tier). Expected: pass. |

### Phase 3 — grammar, passthrough, forwarding

| Task | Content |
| --- | --- |
| **T12** | **Outer argument parser.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/argParser.mjs` implementing CMD-1: outer options (`--port <n>`, `--dry-run`, terminator `--`) recognized only before the first core command token; post-command tokens preserved byte-for-byte in spelling and order (CMD-2); `--publish`, `--expose`, and LAN-exposure flags rejected pre-mutation (PRT-6); CMD-5 start-port grammar — prefix and positional accepted, tail forms `start AGENT --port PORT` and `--port=PORT` rejected before any mutation with both accepted replacements printed; D2 normalization of the forwarded start argv; non-`start` post-command `--port` stays downstream payload. Deps: T01. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxArgGrammar.test.mjs` — table-driven matrix: every outer option name appearing post-command forwards byte-exact (VER-4 collision matrix); all four CMD-5 forms behave as specified; `--` ends outer parsing; rejection messages contain both accepted replacements. Expected: pass. |
| **T13** | **Passthrough entry + routing.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/passthrough.mjs` and `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/commands/route.mjs`: first action is the D3 marker check — inside a box, exec `/opt/ploinky/bin/ploinky-local` with the original argv and never touch engine commands (BIN-4, VER-5); otherwise parse (T12) and dispatch: `help` renders locally with zero engine/box interaction even while engines are `unknown` (CMD-4, IDN-5); `status`/`stop`/`destroy` go to Phase 4 verbs (CMD-8 shadowing at the passthrough only); bare invocation → T14 REPL; `cli` → T14; everything else → ensure box (T10) then forward (T15). A missing, incompatible, or broken box is an explicit failure — never a fallback to direct host execution (N6, CMD-8). Deps: T10, T12. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxCommandRouting.test.mjs` — with a temp marker file injected, the passthrough execs `ploinky-local` with identical argv and records zero engine calls (VER-5 unit); without a marker, verbs dispatch per CMD-4/N6; `help` completes with a fake engine that fails every call (proves zero engine dependency); broken-box forward path exits nonzero with the explicit diagnostic. Expected: pass. |
| **T14** | **REPL + `cli` verbs.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/commands/replCommand.mjs` (bare `ploinky`: ensure box, then interactive engine exec of `/opt/ploinky/bin/ploinky-local` with no args — Q4 as-is REPL, A13) and `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/commands/cliCommand.mjs` (parameterless `ploinky cli`: interactive bash with workdir `/workspace`; `ploinky cli AGENT ARGS...`: forwarded to in-box core `cli` exactly as today's direct CLI handles it, A15) per CMD-3. Deps: T10, T13. |
| | Verification: covered by extending `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxCommandRouting.test.mjs` — fake-engine argv shows `exec -it <instance> /opt/ploinky/bin/ploinky-local` for bare, `exec -it --workdir /workspace <instance> bash` for parameterless `cli`, and byte-exact forward for `cli AGENT --flag`. Expected: pass. |
| **T15** | **Forwarder.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/commands/forwardCommand.mjs`: engine exec of `/opt/ploinky/bin/ploinky-local <argv>` (D4) with TTY-aware `-it`, workdir `/workspace`, byte-exact argv (except the single D2 start-port slot), child exit code propagated. Deps: T10, T12, T13. |
| | Verification: same test file as T13/T14 — forwarded argv equality assertions over the VER-4 matrix, exit-code propagation for fake nonzero exits. Expected: pass. |

### Phase 4 — host-special verbs, locking, canaries

| Task | Content |
| --- | --- |
| **T16** | **`start` flow.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/commands/startCommand.mjs`: under the T04 lock — IDN-5 gate → T10 `ensureBox` (pull → validate → volumes → create by image ID, or reuse/replace per CMD-6) → forward `start AGENT 8080` (D2). `--dry-run` prints the T10 plan and executes nothing (D8). Deps: T04, T10, T12, T15. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxStartFlow.test.mjs` (fake engine) — first start: volumes+create+forward sequence; second start with same config: zero pulls (VER-7), reuse; changed `--port`: IMG-5 replacement path with volume reattachment (VOL-4); forwarded argv `['start','AGENT','8080']` for both accepted CMD-5 forms. Also runnable by hand once Phase 5 lands: `cd "$(mktemp -d)" && /Users/danielsava/work/file-parser/ploinky/bin/ploinky --dry-run --port 9090 start demo/demo` → plan output contains exactly `127.0.0.1:9090:8080/tcp` and `0.0.0.0:7882:7882/udp` and exits 0 without an engine call (VER-1 observable). Expected: pass. |
| **T17** | **`stop` flow.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/commands/stopCommand.mjs`: under the lock — absent or already-stopped box → idempotent success (no prompt, no engine mutation beyond inspect); running box → engine exec `/opt/ploinky/bin/ploinky-local stop` (today's core stop: router down, agent containers stopped, A15) bounded by the T01 graceful timeout, then engine `stop -t <grace> <instance>`; box container and all three named volumes survive (N6, CMD-4). Deps: T04, T10, T15. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxStopStatus.test.mjs` — fake-engine sequence assertion (in-box stop precedes outer stop), repeated stop is a no-op success, no `rm`/`volume rm` recorded (VER-16 unit tier). Expected: pass. |
| **T18** | **`status` flow.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/commands/statusCommand.mjs`: read-only, never acquires the lock (IDN-8), never pulls or reconciles (IMG-3); reports outer box state (exists/running, pinned image ID, recorded logical ref, router host port) and — only when running — appends forwarded core `status` (A15) per CMD-4; absent/stopped box reported as such, exit 0; any `unknown` engine → partial report, exit nonzero (IDN-5). Deps: T03, T10, T11, T15. |
| | Verification: same test file as T17 — fake-engine: status performs zero lock acquisitions (lock dir observed empty during call), zero pulls; unknown-engine fixture yields nonzero exit with partial text; stopped box yields exit 0 with stopped-state text and no start attempt (VER-16 status leg). Expected: pass. |
| **T19** | **`destroy` flow.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/lib/commands/destroyCommand.mjs`: under the lock — missing box → idempotent success, never prompts; otherwise interactive confirmation whose text names the three surviving named volumes, then force-remove of exactly the outer box and its attached anonymous volumes; named volumes and labels survive (VOL-3). Deps: T04, T10. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxDestroy.test.mjs` — fake engine: confirmation prompt text lists all three volume names; piped refusal aborts with zero mutations; piped confirmation records `rm -f` of the container only (no `volume rm`); missing-box destroy exits 0 promptless (VER-8 destroy leg, unit tier). Expected: pass. |
| **T20** | **Lock matrix + canary sweep.** Create `/Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxLockMatrix.test.mjs` (two real child processes contending over one identity with a slow fake engine: mutations serialize; `status`+`help` run to completion while the lock is held by a live holder; stale dead-PID lock recovered exactly once — VER-14) and `/Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxSecretCanaries.test.mjs` (exact canary values in host env and stdin-adjacent surfaces; run start/stop/status/destroy/dry-run against the fake engine; assert canaries absent from every recorded engine argv, every `-e` pair, labels, status output, dry-run output, and captured logs — VER-15). Deps: T16–T19. |
| | Verification: the two commands above, both pass. |

### Phase 5 — packaging

| Task | Content |
| --- | --- |
| **T21** | **Bin rewiring + bin map.** (a) Create `/Users/danielsava/work/file-parser/ploinky/bin/ploinky-local` as a byte-for-byte copy of today's `/Users/danielsava/work/file-parser/ploinky/bin/ploinky` (BIN-1, A8). (b) Rewrite `/Users/danielsava/work/file-parser/ploinky/bin/ploinky` as the thin boxed shim: locate repo root, `exec node <root>/ploinky-box/passthrough.mjs "$@"` — no dependency check, no other logic (BIN-2; the achillesAgentLib check is `ploinky-local`'s and in-box core's concern, A8/A14). (c) Rewrite `/Users/danielsava/work/file-parser/ploinky/bin/psh` to `exec "$SCRIPT_DIR/ploinky-local" sh "$@"` (Q3b). (d) Leave `/Users/danielsava/work/file-parser/ploinky/bin/p-cli` and `/Users/danielsava/work/file-parser/ploinky/bin/ploinky-shell` byte-identical to `BOX_BASE_SHA` (Q3a/Q3c). (e) Create `/Users/danielsava/work/file-parser/ploinky/bin/ploinky-install-deps` per D10 (marker-guarded, installs into the writable deps volume without writing the read-only `/opt/ploinky` root; exact npm flags settled at implementation, risk R8). (f) Edit `/Users/danielsava/work/file-parser/ploinky/package.json` bin map only: remove `p-cloud`, add `"ploinky": "./bin/ploinky"` and `"ploinky-local": "./bin/ploinky-local"` (BIN-5, A12). Deps: T13 (passthrough exists). |
| | Verification: `cmp /Users/danielsava/work/file-parser/ploinky/bin/ploinky-local <(git -C /Users/danielsava/work/file-parser/ploinky show "$BOX_BASE_SHA":bin/ploinky)` → exit 0 (BIN-1 byte-exactness); `cmp /Users/danielsava/work/file-parser/ploinky/bin/p-cli <(git -C /Users/danielsava/work/file-parser/ploinky show "$BOX_BASE_SHA":bin/p-cli)` → exit 0, same for `bin/ploinky-shell`; `node -e 'const b=require("/Users/danielsava/work/file-parser/ploinky/package.json").bin; if (b["p-cloud"]||!b.ploinky||!b["ploinky-local"]) process.exit(1)'` → exit 0; `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxBinShims.test.mjs` (new) — spawns each shim with a controlled PATH (temp dir of symlinks to `bash`, `node`, `dirname`, `readlink`, `realpath` only — no engines): `ploinky-local help` exits 0 (BIN-3); `psh` reaches `cli/shell.js` startup (local routing); `ploinky` without an engine fails with the explicit engine diagnostic, never falling back to host execution (N6); `p-cli --dry-run --port 9191 start x` output equals `ploinky --dry-run --port 9191 start x` output (alias parity, BIN-6). Expected: all pass. |
| **T22** | **VER-3 parity run.** No new files. Two runnable checks: (1) parity — `SHIM_DIR=$(mktemp -d) && ln -s /Users/danielsava/work/file-parser/ploinky/bin/ploinky-local "$SHIM_DIR/ploinky" && cd /Users/danielsava/work/file-parser/ploinky && PATH="$SHIM_DIR:$PATH" npm test` with a container engine available: the existing staged suite (A17–A19) passes unchanged when every `ploinky` invocation resolves to `ploinky-local`; (2) engine-free surface — the T21 controlled-PATH shim test plus `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/*.test.mjs` on a PATH without `podman`/`docker`. Interpretation of VER-3's engine-free clause is recorded as discrepancy R2. Deps: T21. |
| | Verification: command (1) exits 0 printing the suite's `All tests passed!` summary; command (2) exits 0. |

### Phase 6 — entrypoint, smoke harness, real-engine integration

| Task | Content |
| --- | --- |
| **T23** | **Entrypoint asset.** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/entrypoint/ploinky-box-entrypoint.sh` (baked into the image by the cross-repo build, T27): boot self-checks — unprivileged `podman` user identity, writable `/workspace`, executable mounted core (`test -x /opt/ploinky/bin/ploinky-local`), required tools (`bash`, `node`, `npm`, `git`, `podman`), `/dev/fuse` and `/dev/net/tun` present, rootless podman functional with full subordinate UID/GID mappings (`podman unshare` map totals), marker file present with content `6` (IMG-7, D3); VOL-5 boot cleanup — enumerate nested containers whose names match Ploinky's deterministic `ploinky_` prefix (A6) and remove only stale ones; enumeration or removal failure exits nonzero loudly; other nested containers, images, and volumes untouched; then print `PLOINKY-BOX-SELF-CHECK-OK contract=6` and remain as PID 1 keep-alive. Full nested-execution proof (actually running a nested container) belongs to the smoke harness/native gates, not boot. Deps: T01. |
| | Verification: `bash -n /Users/danielsava/work/file-parser/ploinky/ploinky-box/entrypoint/ploinky-box-entrypoint.sh` → exit 0; `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxEntrypoint.test.mjs` (new) — runs the script with a temp PATH of stub `podman`/`node`/`git` shims and fixture filesystem: happy path prints the OK line after cleanup; an injected enumeration failure exits nonzero citing enumeration; a non-`ploinky_` container name is never passed to `rm` (VER-8 boot-cleanup leg, stub tier). Expected: pass. |
| **T24** | **Smoke harness (box smoke tests, LAY-1).** Create `/Users/danielsava/work/file-parser/ploinky/ploinky-box/smoke/smoke-runtime.mjs`: drives the supervisor API (D7) against a real engine and a supplied image (`SMOKE_IMAGE`, default `DEFAULT_IMAGE_REF`; `SMOKE_PORT` for `<routerHostPort>`; agent under test supplied by env/argv — no hardcoded agent IDs in library code, MAN-2): create in an empty temp identity → wait for the T23 OK line → run `/opt/ploinky/bin/ploinky-install-deps` → forwarded `start <agent> 8080` → probe `http://127.0.0.1:<SMOKE_PORT>/health` (A2) and one agent route from the host → in-box `ploinky` no-recursion proof (VER-5 real tier) → in-box `command -v docker` absent / podman-only engine proof (IMG-8, NST-1) → `podman port` shows exactly the two publications → stop matrix (VER-16 real tier) → recreate and prove workspace/deps/nested-image persistence (VER-13 persistence leg, VOL-4) → destroy leaves the three named volumes (VER-8 real tier). This harness is what the publish workflow's native gates invoke (T27). Deps: T10, T16–T19, T21, T23. |
| | Verification (pre-image, structural): `node --test /Users/danielsava/work/file-parser/ploinky/tests/unit/ploinkyBoxSmokeHarness.test.mjs` (new) — harness step-planner unit-tested against the fake engine (step order, probe URLs, cleanup on failure). Full execution is image-gated and lands in Phase 8: `SMOKE_IMAGE=docker.io/assistos/ploinky-box:runtime SMOKE_PORT=18080 SMOKE_AGENT=demo/demo node /Users/danielsava/work/file-parser/ploinky/ploinky-box/smoke/smoke-runtime.mjs` → exit 0. |
| **T25** | **Real-engine integration tests (no product image needed).** Create `/Users/danielsava/work/file-parser/ploinky/tests/integration/ploinkyBoxEngineMatrix.test.mjs` — on a host with rootless podman (skip-with-message otherwise, matching the existing integration-test convention, A20): volume trio creation/labels/foreign-detection/destroy-retention and lock serialization driven through supervisor primitives against a stand-in image (`docker.io/library/alpine` long-sleep; contract validation bypassed only through the D7 injection seam, never a public flag); ownership matrix with a fake `docker` shim on PATH (sole-owner, split fail-closed, podman-first-on-empty — VER-9 real tier). Create `/Users/danielsava/work/file-parser/ploinky/tests/integration/ploinkyBoxReservedPorts.test.mjs` — a helper process occupies UDP `7882`; box creation fails with the PRT-8 diagnostic and no partial resources; create argv/`podman port` contain no `8081` mapping and host `8081` stays closed (VER-10 real tier). Deps: T10, T16–T19. |
| | Verification: `node --test /Users/danielsava/work/file-parser/ploinky/tests/integration/ploinkyBoxEngineMatrix.test.mjs /Users/danielsava/work/file-parser/ploinky/tests/integration/ploinkyBoxReservedPorts.test.mjs` on a podman host → pass (or explicit engine-absent skip lines on hosts without podman). |
| **T26** | **E2E runner.** Create `/Users/danielsava/work/file-parser/ploinky/tests/e2e/ploinkyBox/run.mjs` implementing VER-12 end to end: `mkdir` empty dir → `/Users/danielsava/work/file-parser/ploinky/bin/ploinky start <agent> <port>` → HTTP request through host loopback `<port>` reaches the agent route through the standard proxy pipeline (RTE-1/RTE-7: assert the response traverses the router, e.g. via `/health` + a routed agent path) → `ploinky status` includes in-box agent status → `ploinky stop` → box container + volumes survive → in parallel dir, `/Users/danielsava/work/file-parser/ploinky/bin/ploinky-local start <agent> <port2>` reaches the same route with no box (VER-11 both legs, NST-2 proof obligation). Image-gated: runs only in Phase 8 (or earlier with `SMOKE_IMAGE` pointing at a gated candidate digest). Deps: T21, T24. |
| | Verification: `node /Users/danielsava/work/file-parser/ploinky/tests/e2e/ploinkyBox/run.mjs` → exit 0 with a printed PASS line per leg (deferred to Phase 8 for the boxed leg). |

## 5. Cross-repo coordination (Phase 7 — planned here, executed in `AssistOS-AI/container-image-builds` with owner approval)

This repository's plan **specifies** the following; the edits happen in
`/Users/danielsava/work/file-parser/container-image-builds/` (GitHub
`AssistOS-AI/container-image-builds`) as their own reviewed change, and the
image is produced **only** by triggering the workflow (N7). No Dockerfile or
image-build file enters the ploinky repo.

| # | Edit (all paths relative to the `container-image-builds` repo) | Content |
| --- | --- | --- |
| X1 | `images/ploinky-box/Dockerfile` | Bump `io.assistos.ploinky.runtime-contract` label to `6` (Q2); bake marker `/etc/ploinky-box` with content `6` (D3); `COPY` the entrypoint from the checked-out ploinky source at `sources/ploinky/ploinky-box/entrypoint/ploinky-box-entrypoint.sh` to `/usr/local/bin/ploinky-box-entrypoint` (LAY-1 entrypoint assets; the image stays runtime-only — no other ploinky source is baked, IMG-6); keep the 8-env metadata table exactly as T06 defines it; keep podman as the only container engine on PATH (IMG-8, NST-1); keep `cloudflared` baked (PUB-2 forward-compatibility, deferred slice). |
| X2 | `.github/workflows/publish-ploinky-box-image.yml` — source gate step (today lines 184–207) | Replace the stale `sources/ploinky/container/runtime-contract.mjs` import with `sources/ploinky/ploinky-box/lib/runtimeContract.mjs`, asserting `REQUIRED_RUNTIME_CONTRACT === '6'` (Q2 gate-path migration, same change that mints the version); replace the stale unit-test list (`networkContract.test.mjs` etc. — absent on this branch, A21/A22) with `node --test sources/ploinky/tests/unit/ploinkyBox*.test.mjs`; drop the stale `container/runtime-supervisor-tests.mjs` invocation. |
| X3 | Same workflow — metadata inspect gate (today lines 260–288) | Contract label value `5` → `6`; field set stays in lockstep with the T06 authoritative table (single source of truth: the T06 table is copied verbatim into the jq assertions). |
| X4 | Same workflow — filesystem gate (today lines 290–327) | Keep `test -f /etc/ploinky-box` and add a content check for `6`; keep the `/opt/ploinky/node_modules` empty-dir and source-free checks; the mounted-source gate keeps using `/opt/ploinky/bin/ploinky-install-deps`, now provided by T21e. |
| X5 | Same workflow — lifecycle/publication gates (today lines 359–917) | Replace the retired-layout gates: `sources/ploinky/container/smoke-runtime.mjs` → `node sources/ploinky/ploinky-box/smoke/smoke-runtime.mjs` (T24) parameterized by `SMOKE_IMAGE`/`SMOKE_PORT`/agent input; remove every reference to `cli/services/*` modules and to `/workspace/.ploinky/run/router-health.sock` (both absent from this branch, A22/A23) — router health probes use in-box `http://127.0.0.1:8080/health` (A2); the VER-13 checklist gated natively on both architectures: exact image contract, non-root outer execution, functional nested rootless podman with full subordinate maps, no host engine socket, exactly the two publications, loopback router health, in-box direct-CLI invocation (no recursion), and persistent state across recreation (IMG-9). |
| X6 | Trigger procedure (N7) | After Phases 1–6 are merged and the ploinky commit is pinned: manual `workflow_dispatch` of `publish-ploinky-box-image.yml` supplying the exact 40-character SHAs for all seven source inputs (today lines 4–33), `source_ref` = the pinned ploinky SHA. Both native gate jobs (amd64 on `ubuntu-24.04`, arm64 on `ubuntu-24.04-arm`) must pass before the merge job moves the multi-architecture `runtime` tag (today lines 937–1009). Executed with owner approval; never from this repo. |

## 6. Phase 8 — release gate

| Gate | Check | Command / evidence | Expected |
| --- | --- | --- | --- |
| G1 | VER-13 multi-architecture | The X5/X6 native gate jobs `Build and gate linux/amd64` and `Build and gate linux/arm64` plus the `Publish mutable multiarchitecture runtime` merge job in `publish-ploinky-box-image.yml` | All three jobs green; `runtime` tag digest printed by the final inspect step lists `linux/amd64` and `linux/arm64` |
| G2 | VER-12 end-to-end smoke | `node /Users/danielsava/work/file-parser/ploinky/tests/e2e/ploinkyBox/run.mjs` on a Linux host with rootless podman (see risk R9 for macOS) | Exit 0: empty dir → `ploinky start <agent>` → host-loopback request reaches the agent route; `ploinky-local` leg reaches the same route with no box |
| G3 | Existing surface regression | `cd /Users/danielsava/work/file-parser/ploinky && npm test` and the T22 parity variant | Suite summary `All tests passed!`, exit 0 |
| G4 | Additive boundary (VER-2 final) | `git -C /Users/danielsava/work/file-parser/ploinky diff --name-only "$BOX_BASE_SHA"..HEAD -- cli/ Agent/ dashboard/` and `git -C /Users/danielsava/work/file-parser/ploinky diff --name-only "$BOX_BASE_SHA"..HEAD \| grep -v -E '^(ploinky-box/\|tests/\|bin/\|package\.json\|docs/superpowers/)'` | Both print nothing |
| G5 | VER coverage sign-off | Section 7 matrix walked row by row with each named command re-run | Every row's command(s) pass |
| G6 | IMG-2 hard-cut notice | Release note text (register section 8): the first contract-6 publish moves `docker.io/assistos/ploinky-box:runtime` off contract `3` for every existing box; old boxes fail closed with the destroy-and-recreate instruction; volumes created by retired tooling carry no `io.assistos.ploinky-box.*` labels and are therefore foreign (VOL-2) — never adopted, never deleted, manual recovery only. No migration, translation, or dual-read exists. | Notice published alongside the trigger record |

## 7. VER coverage matrix

| VER | Obligation (abbrev.) | Covered by tasks | Where it runs |
| --- | --- | --- | --- |
| VER-1 | Exactly two `-p` mappings; `--port` host-side only; LAN flags rejected | T07, T10, T12, T16 | `tests/unit/ploinkyBoxPublications.test.mjs`, `tests/unit/ploinkyBoxSupervisor.test.mjs`, `tests/unit/ploinkyBoxArgGrammar.test.mjs`; observable via the T16 `--dry-run` command |
| VER-2 | Additive boundary + static import/purity checks | T05, T21, G4 | `tests/unit/ploinkyBoxBoundary.test.mjs`; phase-exit and G4 diff gates |
| VER-3 | Existing suite via `ploinky-local`; no engine requirement added | T21, T22 | T22 parity `npm test` run; `tests/unit/ploinkyBoxBinShims.test.mjs` (interpretation: R2) |
| VER-4 | Byte-exact forwarding, collision matrix | T12, T15 | `tests/unit/ploinkyBoxArgGrammar.test.mjs`, `tests/unit/ploinkyBoxCommandRouting.test.mjs` |
| VER-5 | Marker: forwarded commands take direct CLI path; no recursion in-box | T13 (unit), T24 (real) | `tests/unit/ploinkyBoxCommandRouting.test.mjs`; smoke step in `ploinky-box/smoke/smoke-runtime.mjs` |
| VER-6 | Field-specific fixture rejection; unsupported version instructs destroy/recreate, mutates nothing | T06 | `tests/unit/ploinkyBoxRuntimeContract.test.mjs` + `tests/fixtures/ploinky-box/` |
| VER-7 | Pull matrix; run by image ID; no registry traffic on reuse/status/stop/destroy | T09, T16 | `tests/unit/ploinkyBoxImageLifecycle.test.mjs`, `tests/unit/ploinkyBoxStartFlow.test.mjs` |
| VER-8 | Destroy retains labelled volumes; recreation reattaches; boot cleanup only `ploinky_` prefix | T08, T19 (unit), T23 (stub), T25, T24 (real) | unit files above; `tests/integration/ploinkyBoxEngineMatrix.test.mjs`; smoke persistence/destroy steps |
| VER-9 | Engine matrix: unknown-blocking, sole-owner, split fail-closed, podman-first-on-empty | T03 (unit), T25 (real) | `tests/unit/ploinkyBoxEngineDiscovery.test.mjs`; `tests/integration/ploinkyBoxEngineMatrix.test.mjs` |
| VER-10 | `8081` never mapped/reachable; `7882` occupancy → collision diagnostic | T07, T10 (unit), T25 (real) | unit files above; `tests/integration/ploinkyBoxReservedPorts.test.mjs` |
| VER-11 | PRT-10 env wiring makes host-loopback reach the in-box router; `ploinky-local` keeps loopback defaults | T10 (unit argv), T26 (real) | `tests/unit/ploinkyBoxSupervisor.test.mjs`; `tests/e2e/ploinkyBox/run.mjs` both legs |
| VER-12 | Empty dir → boxed start → loopback request reaches agent route; local leg without box | T26, G2 | `node /Users/danielsava/work/file-parser/ploinky/tests/e2e/ploinkyBox/run.mjs` |
| VER-13 | Native amd64+arm64 gates: contract, nested rootless podman, two publications, loopback health, in-box direct CLI, persistence | T24 (harness), X5/X6, G1 | Runs **outside this repo**, inside the publish workflow's `Build and gate linux/amd64` / `Build and gate linux/arm64` jobs invoking `sources/ploinky/ploinky-box/smoke/smoke-runtime.mjs`; merge job completes the gate |
| VER-14 | Lock serialization; stale-lock proof-of-death; `status`/`help` lock-free | T04, T18, T20 | `tests/unit/ploinkyBoxMutationLock.test.mjs`, `tests/unit/ploinkyBoxLockMatrix.test.mjs`, `tests/unit/ploinkyBoxStopStatus.test.mjs` |
| VER-15 | Secret canaries absent from every engine-visible surface | T11, T20 | `tests/unit/ploinkyBoxSecretHygiene.test.mjs`, `tests/unit/ploinkyBoxSecretCanaries.test.mjs` |
| VER-16 | Stop matrix: ordered graceful stop, idempotence, survival of container+volumes; status before/after | T17, T18 (unit), T24/T26 (real) | `tests/unit/ploinkyBoxStopStatus.test.mjs`; smoke and e2e stop/status steps |

All sixteen obligations are covered; VER-13 is the only one whose
authoritative run happens outside this repository (named workflow jobs above),
with its harness (T24) developed and unit-planned here.

## 8. Risks, open questions, register discrepancies

| ID | Item | Treatment |
| --- | --- | --- |
| Q1 | Bridge-networked nested agent → private listener `8081` under rootless nested podman | Stays open; owned by the routing layer. The base slice publishes nothing on `8081` (PRT-9, VER-10) and binds no outer interface for it. Bridge-mode private machine calls are **not declared supported**; no task in this plan depends on resolving this. If the smoke/e2e work surfaces it, record findings under the open question — do not patch core (N5). |
| Q5 | UDP media consumer sequencing | Out of scope. The `0.0.0.0:7882:7882/udp` reservation exists from day one (T07); an idle drop-packets mapping is the accepted steady state. |
| Q6 | Nested-podman issue not fixable at image level | Escalation rule restated (NST-4, N5): stop, document the exact failing invocation and error verbatim, and obtain an owner decision; core is never patched for it and the additive-layer rule is never silently broken. |
| R1 | Register tension: N5/VER-2's letter ("only `bin/*`, `package.json` outside `ploinky-box/`") vs section 16's requirement that every check be runnable in `tests/unit`/`tests/integration`/`tests/e2e` | Resolved as D1: additive test files under `/Users/danielsava/work/file-parser/ploinky/tests/` are sanctioned by section 16 itself (they are the only way its obligation can be met; the unit stage auto-discovers them with zero harness edits, A18). The enforced boundary (G4) whitelists `ploinky-box/`, `tests/`, `bin/*`, `package.json`, `docs/superpowers/`; the `cli/`/`Agent/`/`dashboard/` empty-diff rule is absolute. Owner may override by relocating tests, at the cost of amending section 16. |
| R2 | VER-3's literal "passes unchanged … with no container engine on PATH" cannot mean the full staged suite: today's suite starts real agent containers, and core itself exits without an engine (A7) | Interpreted as two checks (T22): full-suite parity driven through `ploinky-local` (engine present), plus an engine-free assertion that `ploinky-local` adds no engine requirement (BIN-3) covering `help` and the unit stage. Flagged for owner confirmation. |
| R3 | IDN-4's three-value enum (`owns`/`absent`/`unknown`) has no value for "installed, reachable, zero owned resources", yet IDN-6's podman-first rule needs exactly that state | T03 models the probe as reachability × ownership and projects onto the register enum, adding `reachable-empty` internally. Semantics of IDN-5/IDN-6 are preserved exactly. Flagged as a register wording gap, not an owner decision. |
| R4 | CMD-2 byte-exact forwarding vs CMD-5/PRT-1/PRT-6 port semantics | Resolved as D2: exactly one token (the start port operand) is normalized to `8080`; everything else is byte-exact and VER-4-tested. Flagged for owner confirmation since CMD-2's letter admits no exception. |
| R5 | Stale references in the current publish workflow: `container/` gate paths (A21), `cli/services/*` modules (A22), `/workspace/.ploinky/run/router-health.sock` (A23), a unit-test list absent from this branch, and `bin/ploinky-install-deps` expected but not yet present | All addressed by X1–X5 (gate migration to `ploinky-box/`, health via `/health`, T21e provides the deps helper). Until Phase 7 executes, the workflow cannot gate this branch — publishing before Phase 7 is impossible, which is consistent with the Phase 8 ordering. |
| R6 | Docker Hub contract-3 fact is prompt-provided (dated 2026-07-21), not re-verified locally in this session | Immaterial to correctness: IMG-2 treats every non-`6` label as unsupported with the same fail-closed instruction. Re-inspect the live tag before the X6 trigger as a courtesy check. |
| R7 | Exact `PLOINKY_PUBLIC_AUTHORITY` value the box must set (`127.0.0.1:<routerHostPort>` proposed) depends on `normalizeAuthority` semantics not exercised in this planning pass | Settle during T10 implementation by reading `normalizeAuthority` in `/Users/danielsava/work/file-parser/ploinky/cli/server/RoutingServer.js` (consumption only — no edits); VER-11's e2e leg is the binding proof. |
| R8 | Exact npm invocation for `bin/ploinky-install-deps` against a read-only `/opt/ploinky` with writable `node_modules` volume (lockfile/save behavior, postinstall clone into the volume) | Settle during T21e with in-box trials; the workflow's mounted-source gate (X4) is the acceptance check. |
| R9 | macOS development hosts run podman inside a VM; `/dev/fuse`, `/dev/net/tun`, and rootless nesting behavior may differ from native Linux | Integration (T25) and e2e (T26/G2) target a native Linux host; the authoritative multi-arch proof is G1 (workflow native runners). macOS runs are best-effort local convenience only. |
| R10 | Pre-existing uncommitted modifications on `ploinky-proxy` (section 0) would contaminate the VER-2 measurement if not parked first | Mitigated by the `BOX_BASE_SHA` discipline; T05/G4 commands are pinned to that SHA. |
| R11 | IMG-5 rollback needs the prior creation configuration after the old box is removed | T09 snapshots the old container's inspect output (image ID, env, labels, ports, mounts) before removal and rebuilds create args from the snapshot; unit-tested in the T09 failure-injection matrix. |

## 9. What done looks like

An engineer starts at Phase 1 / T01 with `BOX_BASE_SHA` recorded and works
tasks in numeric order; every task names its exact files, its dependencies,
and a runnable command whose expected result is stated. No architectural
decision remains open inside the build scope: the only "proposed" rows
(section 2) default to the stated choice unless the owner overrides them
before the affected task starts. The plan ends when all six G-gates in
section 6 pass and every row of the section 7 matrix has a green command
attached.
