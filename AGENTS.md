# Pointer

The canonical instructions for this repository live in `CLAUDE.md` in this directory.

Read [CLAUDE.md](./CLAUDE.md). This pointer intentionally contains no duplicate policy so the Ploinky invariants and mandatory `ploinky-proxy` acceptance gate cannot drift between instruction files.
